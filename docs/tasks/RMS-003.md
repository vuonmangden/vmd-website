# RMS-003 — Rate Rules (Sandbox)

- **Task ID:** `RMS-003`
- **Trạng thái:** Done (`SYNTHETIC_ONLY`)
- **Phụ thuộc:** `RMS-001`, `MNT-014`
- **Mục tiêu:** Schema/API nội bộ cho rule giá theo khoảng ngày, thứ trong tuần, priority và giới hạn số đêm; tất cả monetary values chỉ là fixture sandbox.
- **Cấm:** công bố giá, cọc, phụ thu, thuế/phí, voucher hoặc chính sách production; `PRE-003` và `PRE-005` vẫn Blocked.
- **Security:** `room.manage`, integer VND only, validation date/range/nightly price; no frontend trust; synthetic seed opt-in only.
- **Xác minh local:** `prisma validate`; API lint/typecheck/test (72/72); synthetic fixture test (8/8); migration deploy trên PostgreSQL Docker trắng và deploy lần 2 không có pending migration; seed chạy hai lần an toàn.
- **Hosted CI:** GitHub Actions run `31598071210` đạt. Không thay đổi trạng thái `PRE-003`/`PRE-005`.
