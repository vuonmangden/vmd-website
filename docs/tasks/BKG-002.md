# BKG-002 — Occupancy Model (Sandbox)

- **Task ID:** `BKG-002`; **Status:** In progress (`SYNTHETIC_ONLY`)
- **Dependencies:** `RMS-002`, `MNT-014`
- **Scope:** PostgreSQL `room_occupancies` daily inventory guard, unique `(room_id, stay_date)`. Booking table is not available yet, so `booking_id` is an opaque UUID reference without a foreign key until BKG-004.
- **Rules:** one row per stay night; valid statuses `HOLD`/`CONFIRMED`; the unique constraint is the final collision guard; no public API or booking confirmation in this task.
- **Done:** forward migration, service inserts a date range transactionally and maps unique collision to `BOOKING_CONFLICT`; integration verification and full gates.
