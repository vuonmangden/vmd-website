# MNT-010 — PRE-007 Staging-only Implementation Gate

## 1. Thông tin task

- **Task ID:** `MNT-010`
- **Trạng thái:** Done
- **Dependency:** `MNT-009`
- **Mục tiêu:** Ghi nhận quyết định staging-only để mở `IAM-001` và `NTF-002` mà không dùng staging làm production.

## 2. Phạm vi

- Cập nhật PRE-007, tracker, readiness matrix và hai task spec với các input staging/email mới nhất.
- Khóa production fail-closed: Supabase production phải được tạo riêng trong `REL-001` trước go-live; Resend production chỉ được bật sau khi DNS/identity được xác minh.
- Ghi nhận `RESEND_API_KEY` là reference secret trong Railway Variables, không ghi giá trị secret vào Git.

## 3. Ngoài phạm vi

- Không tạo Supabase production project, DNS record, Railway Variable hoặc Resend account/configuration.
- Không triển khai IAM/email adapter, không thêm dependency, API, migration hay production code.
- Không đưa bounce/complaint webhook vào `NTF-002`; phần này thuộc `NTF-007` hoặc OPS task sau.

## 4. Acceptance criteria

- `IAM-001` và `NTF-002` chuyển `Ready` chỉ trong phạm vi staging-only, với owner/file scope không chồng lấp.
- Production dùng Supabase riêng qua `REL-001`; staging project không được cấu hình/deploy như production.
- Email production không gửi được khi chưa có cấu hình hợp lệ hoặc khi domain còn chưa xác minh.
- `git diff --check` và quality gate đạt.

## 5. Evidence

- Chủ dự án xác nhận staging project, `RESEND_API_KEY`, Resend test mode/Mailpit và scope webhook/bounce ngày 2026-08-11.
- Không tạo hoặc thay đổi Supabase, DNS, Railway Variables hay Resend; không có secret, API, migration, dependency hoặc production code.
- Node 24.14.0/pnpm 11.9.0: `git diff --check`, full lint, typecheck, test (19 pass, 1 conditional integration skip) và build đạt; Prisma config regression chạy sau warm-up Windows.
- PR #11: GitHub-hosted Quality, schema/Compose và Secret/dependency gates đạt ở run `31462567769`.
