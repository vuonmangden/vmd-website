# RMS-004 — Room Blocks (Sandbox)

- **Task ID:** `RMS-004`
- **Trạng thái:** Done (`SYNTHETIC_ONLY`)
- **Phụ thuộc:** `RMS-002`, `MNT-014`
- **Mục tiêu:** Cho nhân sự có quyền `room.manage` tạo và hủy block theo từng phòng để phục vụ bảo trì hoặc sử dụng nội bộ trong môi trường sandbox.
- **Schema/API:** `room_blocks` gồm room, khoảng ngày, lý do, block type, người tạo, thời điểm tạo/hủy; `POST /admin/rooms/{id}/blocks` và `DELETE /admin/rooms/{id}/blocks/{blockId}` theo Tech Spec §10.10, §33.
- **Bắt buộc:** range ngày hợp lệ; xác thực phòng chưa xóa; actor lấy từ server-side auth context; integer/date UTC storage; migration forward-only; fixture synthetic opt-in và fail-closed production.
- **Cấm:** tự suy diễn quy tắc vận hành thực tế, public booking UI, occupancy/hold/booking state machine, hoặc sử dụng dữ liệu production. Không thay đổi `PRE-002`.
- **Done:** migration, service/controller, tests validation/auth behavior, fixture idempotent, PostgreSQL Docker verification, full local gates và hosted CI.
- **Xác minh local:** Prisma validate; full lint/typecheck/test/build; fixture synthetic 8/8; migration deploy PostgreSQL Docker trắng và deploy lần 2 không có pending migration; seed chạy hai lần an toàn.
- **Hosted CI:** GitHub Actions run `31599535588` đạt.
