# MNT-007 — PRE-007 Identity Intake and Gate Review

## 1. Thông tin task

- **Task ID:** `MNT-007`
- **Trạng thái:** Done
- **Dependency:** `MNT-004`
- **Mục tiêu:** Ghi nhận dữ liệu domain/tích hợp do chủ dự án cung cấp, đánh giá chính xác quyền mở `IAM-001` và không suy đoán cấu hình production.

## 2. Phạm vi

- Ghi Supabase development/staging, JWT/JWKS, CORS, callback, phương thức đăng nhập, session, MFA và secret-management metadata vào PRE-007.
- Cập nhật tracker/readiness matrix theo từng gate của `IAM-001` và `NTF-002`.
- Nêu rõ phần còn thiếu, không biến environment development/staging thành production bằng giả định.

## 3. Ngoài phạm vi

- Không triển khai `IAM-001`, `NTF-002`, SePay, Zalo, email provider hoặc hosting.
- Không đăng nhập Supabase, không xác minh bằng secret, không tạo/chỉnh provider configuration và không ghi secret vào Git.
- Không thêm API, migration, dependency hoặc thay đổi production code.

## 4. Acceptance criteria

- Tất cả dữ liệu không nhạy cảm từ chủ dự án ngày 2026-08-10 được lưu nguyên nghĩa, có provenance và không chứa secret.
- `IAM-001` vẫn planning-only nếu production project/domain/allowed URLs chưa được duyệt.
- `NTF-002` vẫn planning-only nếu email provider/from identity chưa có.
- `git diff --check` và các quality gates dự án đạt.

## 5. Evidence

- Chủ dự án cung cấp identity development/staging metadata ngày 2026-08-10; chỉ identifier/reference được ghi lại.
- Không Supabase login, không secret, không API, migration, dependency hoặc production code.
- Node 24.14.0/pnpm 11.9.0: full lint, typecheck, test (19 pass, 1 conditional integration skip) và build đạt. Prisma config regression được warm trước khi chạy để tránh timeout cold-start Windows; test thực thi đạt trong khoảng 3 giây.
- `IAM-001` và `NTF-002` vẫn ở planning-only cho đến khi các gate production/provider trong PRE-007 được duyệt.
