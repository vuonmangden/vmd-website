# IAM-004 — Audit Service

## 1. Thông tin task

- **Task ID:** `IAM-004`
- **Tên task:** Audit Service
- **Trạng thái:** In progress
- **Branch:** `feat/iam-004-audit-service`
- **Dependency:** FND-005 (audit_logs table), IAM-001

## 2. Mục tiêu

Cung cấp AuditService để ghi lại mọi thao tác nhạy cảm (booking, payment, staff changes) theo Tech Spec §12 (Logging và Audit). Immutable — không cho phép sửa/xóa audit log.

## 3. Phạm vi

- AuditService: log(), findByResource(), findByActor().
- AuditModule exported cho các module khác inject.
- Unit tests.

## 4. Definition of Done

- AuditService ghi audit_logs với đầy đủ context (actor, action, resource, before/after, correlation).
- Unit tests pass.
- Quality gates pass.
