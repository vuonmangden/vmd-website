# IAM-004 — Audit Service

## 1. Thông tin task

- **Task ID:** `IAM-004`
- **Mức ưu tiên:** P0
- **Trạng thái:** Backlog (`PLANNING_ONLY`)
- **Phụ thuộc:** `FND-005`, `IAM-001`; audit viewer phụ thuộc `IAM-002`
- **Branch dự kiến:** `codex/iam-004-audit-service`

## 2. Mục tiêu và phạm vi

- Tạo audit service dùng chung ghi actor, action, resource, before/after đã lọc, reason, IP, user-agent, timestamp và correlation ID.
- Dùng bảng `audit_logs` bất biến hiện có; chỉ tạo migration mới nếu task review chứng minh schema hiện tại thiếu bắt buộc.
- Thêm `GET /admin/audit-logs` có pagination/filter và `audit.read`; không cho API sửa/xóa audit.

## 3. Quy tắc và tests

- Audit bắt buộc cho auth/permission failure, role assignment, setting và làm hook cho booking/price/payment/refund về sau.
- Không lưu password, token, OTP, secret hoặc PII đầy đủ; lỗi audit ở thao tác bắt buộc phải fail an toàn, không swallow.
- Test immutability, masking, actor server-side, pagination/authz/IDOR và correlation ID.
- OpenAPI, full lint/typecheck/test/build, database verification nếu có migration và hosted CI đạt.

