# IAM-003 — Admin Route Protection

## 1. Thông tin task

- **Task ID:** `IAM-003`
- **Tên task:** Admin Route Protection
- **Trạng thái:** In progress
- **Branch:** `feat/iam-003-admin-route-protection`
- **Dependency:** IAM-002 (Done)

## 2. Mục tiêu

Tạo composite AdminGuard kết hợp JWT auth + role/permission check, sẵn sàng apply cho tất cả admin endpoints. Mặc định từ chối nếu chưa xác thực.

## 3. Phạm vi

- `AdminGuard` — composite guard: authenticate → check roles → check permissions.
- Export từ AuthModule để các module khác dùng.
- Unit tests.

## 4. Definition of Done

- AdminGuard authenticate + authorize trong một guard.
- Unauthenticated → 401, unauthorized → 403.
- Quality gates pass.
