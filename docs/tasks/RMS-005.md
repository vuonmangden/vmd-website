# RMS-005 — Price Engine (Sandbox)

- **Task ID:** `RMS-005`; **Status:** In progress (`SYNTHETIC_ONLY`)
- **Dependencies:** `RMS-003`, `MNT-014`
- **Scope:** Backend-only deterministic calculation of nightly room subtotal and synthetic extra-adult/child amounts from active `room_rate_rules`; VND integers only.
- **Forbidden:** deposit/payable-now, tax, surcharge, add-ons, BBQ, vouchers, public price display or any production policy. `PRE-003` and `PRE-005` remain Blocked.
- **Rule selection:** active rules within `[dateFrom, dateTo)`, matching weekday where configured; highest `priority` wins, then stable ID tie-break. Invalid date/guest input or missing matching rule fails closed.
- **Done:** unit tests for range/priority/weekdays/guests/invalid input, API internal contract if added, full gates and hosted CI.
- **Xác minh local:** full lint/typecheck/test/build và Prisma validate đạt; Price Engine + repository tests 5/5.
- **Chờ hoàn tất:** hosted CI và PR review.
