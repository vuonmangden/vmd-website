# MNT-004 — Planning-Only Feature Lane Specifications

## 1. Thông tin task

- **Task ID:** `MNT-004`
- **Trạng thái:** Done
- **Dependency:** `MNT-003`, `TST-001`
- **Phê duyệt:** Chủ dự án yêu cầu tiếp tục theo thứ tự tăng tốc đã chốt ngày 2026-08-09.
- **Mục tiêu:** Khóa contract, file ownership, PRE gate và tiêu chí kiểm thử cho ba lane kế tiếp mà không triển khai production code khi dữ liệu thật chưa sẵn sàng.

## 2. Phạm vi

- Tạo task spec planning-only cho `IAM-001`, `CMS-005` và `NTF-002`.
- Ghi rõ đầu vào tối thiểu cần duyệt từ `PRE-007`/`PRE-008`.
- Tách file ownership để ba lane có thể chạy song song sau khi từng gate đạt.
- Cập nhật readiness matrix và progress tracker.

## 3. Ngoài phạm vi

- Không sửa production code, API, schema, migration, dependency hoặc cấu hình provider.
- Không dùng synthetic fixture để đóng `PRE-007`, `PRE-008` hoặc `BLK-001`.
- Không tạo branch implementation cho ba feature task.

## 4. Acceptance criteria

- Mỗi task có dependency, phạm vi, ngoài phạm vi, file ownership, PRE checklist, security, tests và Definition of Done.
- IAM/CMS/Email không sở hữu trùng file hoặc database verification.
- Readiness matrix tiếp tục ghi cả ba task là `PLANNING_ONLY`.
- `git diff --check` và required GitHub checks đạt; PR được merge qua branch protection.

## 5. Evidence

- Tạo đủ ba planning-only spec, không thay đổi production code, migration hoặc dependency.
- Local Node 24.14.0/pnpm 11.9.0: lint 12/12, typecheck 12/12, test 12/12 + script tests 19 pass/1 integration skip có điều kiện, build 12/12.
- `git diff --check` đạt.
