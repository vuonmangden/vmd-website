# RMS-002 — Physical Rooms (Synthetic/Staging-only)

- **Task ID:** `RMS-002`
- **Trạng thái:** Review (`IMPLEMENTATION_APPROVED — SYNTHETIC_ONLY`)
- **Phụ thuộc:** `RMS-001`, `MNT-014`
- **Phạm vi:** schema/API nội bộ cho phòng vật lý: code unique, Room Type FK, tên/khu vực/trạng thái/bảo trì, soft delete và fixture `SYNTHETIC` guarded.
- **Ngoài phạm vi:** room inventory thật, giá/cọc/policy, occupancy, booking, public UI và production seed.
- **Security:** tất cả endpoint admin yêu cầu trusted actor + `room.manage`; fixture fail-closed ngoài local/development/test/demo.
- **Done:** migration blank/deploy x2; FK/unique/status; CRUD soft-delete; fixture idempotent/production reject; lint/typecheck/test/build/Prisma/hosted CI.

## Evidence — 2026-08-12

- Migration `20260812100000_add_rooms`: Room Type FK, unique room code, status constraint, lookup index and soft delete.
- Protected admin API `GET/POST /admin/rooms`, `POST /admin/rooms/:id/archive`; trusted actor + `room.manage` required.
- Fixture `SYNTHETIC-ROOM-001` is idempotent and is rejected outside explicit non-production authorization.
- Isolated PostgreSQL 16 verification: blank deploy 7 migrations, second deploy no pending, seed twice yields one synthetic Room Type + Room.
- API 68/68, fixture 8/8 and full lint/typecheck/test/build/Prisma gates pass.
